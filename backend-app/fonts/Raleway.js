import { Raleway } from "next/font/google";

// Raleway por defecto
export const raleway = Raleway({
    weight: "400",
    subsets: ["latin"]
});

// Raleway más fuerte
export const ralewayS = Raleway({
    weight: "700",
    subsets: ["latin"]
});